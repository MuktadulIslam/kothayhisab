import 'package:flutter/material.dart';
import 'package:kothayhisab/app.dart';
import 'package:kothayhisab/data/api/services2/storage/shared_prefs_service.dart';

void main() async {
  // // start:::for next version
  // // Ensure Flutter widgets are initialized
  // WidgetsFlutterBinding.ensureInitialized();

  // // Initialize shared preferences service
  // await SharedPrefsService().init();
  // // end:::for next version

  runApp(const KothayHisabApp());
}
